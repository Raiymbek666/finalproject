CREATE TABLE access (
    id int PRIMARY KEY,
    name varchar(100),
    surname varchar(100),
    login varchar(100) PRIMARY KEY,
    password varchar(100)
)


CREATE TABLE cars (
    car_id INT PRIMARY KEY,
    mark varchar(100),
    model varchar(100),
    year int,
    color varchar(100)
)